// STEP 1 of setup: copy this file to "firebase-config.js" (same folder) and
// paste in the values from your own Firebase project — see README.md,
// section "1. Create your Firebase project", for exactly where to find them.
//
// This file is safe to commit to GitHub / make public. A Firebase "web app
// config" is not a secret key — it just tells the browser which Firebase
// project to talk to. Who can read/write your data is controlled separately,
// by the Firestore security rules you'll paste in during setup (README
// section 3), not by hiding this file.

const firebaseConfig = {
  apiKey: "PASTE-YOUR-API-KEY-HERE",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:xxxxxxxxxxxxxxxxxxxxxx"
};
